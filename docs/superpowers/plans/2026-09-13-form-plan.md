# FORM Implementation Plan
Goal: animated chest exercise learning page.
Architecture: static HTML/CSS/ES modules with locally vendored Three.js and OrbitControls.
Spec: docs/superpowers/specs/2026-09-13-form-design.md

1. Create tests/motion.test.mjs to verify all nine poses remain finite, elbows preserve arm lengths, and push-up contacts stay fixed. Run before dist/motion.js exists to establish the missing implementation failure.
2. Create dist/exercises.js with nine explicit labels and exercise-specific setup, action, breathing, cues and muscle targets. Create dist/motion.js exporting solveArm and poseAt.
3. Create dist/index.html and dist/style.css for exercise rail, studio canvas, accessible playback controls, and coaching panel. Create dist/scene.js for procedural athlete, equipment, lighting and camera. Create dist/app.js to synchronize selected exercise, phase, controls and explanations.
4. Run node --test tests/motion.test.mjs and JavaScript syntax checks. Inspect local asset paths and exercise coverage. Fix concrete failures. No browser QA was requested; managed preview remains off.
5. Save source to the Site repository and publish privately through Sites; verify terminal deployment success.
